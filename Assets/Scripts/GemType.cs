namespace Match3
{
    // Note: Using "Special" enum for now, can be changed to bomb, arrow etc. if necessary
    // Also Flags can be used
    public enum GemType
    {
        Blue = 0,
        Green = 1,
        Red = 2,
        Yellow = 3,
        Purple = 4,
        Special = 5,
    };
}